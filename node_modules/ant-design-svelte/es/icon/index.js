import * as IconExit from './icon';
import { default as Icon } from './icon';
Object.keys(IconExit).forEach(function (key) {
    if (key !== 'default') {
        Icon[key] = IconExit[key];
    }
});
export default Icon;
